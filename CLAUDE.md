# orariogen — traslocato

⚠ **Questo repository non contiene più il generatore.** Il codice, i test e i
documenti sono dentro `Mactywd/aurora` dal 2026-09-01: `backend/orario/` per il
codice e i test, `docs/orario/` per i documenti.

**Lo stato corrente, le convenzioni e l'indice stanno in
`backend/orario/CLAUDE.md` di quel repository**, non qui: qualunque cosa fosse
scritta in questo file sarebbe una seconda verità che diverge dalla prima.

Il perché del trasloco è in [README.md](README.md), e per esteso negli ADR-027
e ADR-032 (`docs/orario/decisioni.md` di Aurora).

Qui resta la **storia**: i commit fino al trasloco, e `.superpowers/sdd/`.
